import 'package:flutter/material.dart';

class SchedulePageModalAdd extends StatelessWidget {
  const SchedulePageModalAdd({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
